export type StatCard = {
  label: string;
  value: string;
  description: string;
};

export type Program = {
  title: string;
  track: string;
  summary: string;
};

export type EventItem = {
  name: string;
  date: string;
  location: string;
};

export type ContactChannel = {
  label: string;
  detail: string;
  helper: string;
};
